name: Deploy Neon FieldGoal Web

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "neon-fieldgoal-web/**"
      - ".github/workflows/neon-fieldgoal-web-pages.yml"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "neon-fieldgoal-web-pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: neon-fieldgoal-web

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
